# moodit QA Findings

> 작성일: 2026-05-09 KST  
> 상태: Current  
> 기준 문서: [SCREEN_ACTIONS_QA_DEFINITION.md](./SCREEN_ACTIONS_QA_DEFINITION.md), [QA_TEST_PLAN.md](./QA_TEST_PLAN.md)

## Completion Audit

| Requirement | Evidence | Status |
|---|---|---|
| 모든 앱 화면과 액션을 먼저 정의 | `SCREEN_ACTIONS_QA_DEFINITION.md`가 `AppRoute` 전체 케이스와 root tab, permission-only 화면을 포함 | Done |
| 버튼/액션 흐름과 SwiftUI 매핑 기준 확보 | `SCREEN_ACTIONS_QA_DEFINITION.md`의 Screen Registry와 `QA_TEST_PLAN.md`의 수동 QA 체크리스트 | Done |
| 자동 QA 실행 | `./scripts/test.sh` PASS, unit suites + 26 AppUITests, xcresult `Test-moodit-2026.05.09_00-44-00-+0900.xcresult` | Done |
| Backend callable unit QA | `npm --prefix functions test` PASS, 57 tests | Done |
| Firestore Rules QA | `npm --prefix functions run test:rules` PASS with Firestore emulator, 11 tests | Done |
| 누락된 액션/화면 selector 보정 | `ActionSurfaceSmokeTests`, `P0CoreActionTests`와 관련 SwiftUI accessibility ID 보강 | Done |
| 실패한 automated QA 수정 | paywall UI-test Firebase 호출 우회, filter download cancel, help center/capture preview/permission route 보강 | Done |
| 화면/핵심 액션 텔레메트리 | `Telemetry.fmTrackScreen` modifier가 root tab, camera cover, 모든 `AppRoute` destination의 진입/이탈 duration을 기록. Marketplace/Search/FilterDetail/Camera 핵심 funnel/action/empty/error/refresh 이벤트 추가 | Done |
| UI Test helper consolidation | `MooditUITestCase` 공통 launch/tap/assert/helper를 추가하고 `ActionSurfaceSmokeTests`, `PhaseAE2ETests`, `PhaseDE2ETests`, `P0CoreActionTests`의 중복 헬퍼를 제거 | Done |
| Workflow helper extraction | `WorkflowScreens.swift`의 공용 workflow UI/formatter/editor asset helper를 `WorkflowHelpers.swift`, `WorkflowFormatters.swift`, `EditorAssets.swift`로 분리하고 UI-test 전용 분기를 `#if DEBUG`로 제한 | Done |
| 실기기/외부 서비스 QA | Apple/Google Sign-In, StoreKit sandbox, 실제 카메라 촬영/저장/공유, APNs/FCM push, App Settings deep link | Blocked |

## Automated Verification

| Date | Command | Result |
|---|---|---|
| 2026-05-09 | `xcodebuild ... -only-testing:AppUITests/ActionSurfaceSmokeTests/testMarketplaceSupportPermissionAndPreviewSurfaces` | PASS, 1 test |
| 2026-05-09 | `xcodebuild ... -only-testing:AppUITests/ActionSurfaceSmokeTests` | PASS, 5 tests, includes `filter.detail.tags` |
| 2026-05-09 | `./scripts/test.sh` | PASS, unit suites + 26 AppUITests |
| 2026-05-09 | `npm --prefix functions test` | PASS, 57 tests |
| 2026-05-09 | `env XDG_CONFIG_HOME=/private/tmp/firebase-config FIREBASE_CLI_DISABLE_UPDATE_CHECK=true npm --prefix functions run test:rules` | PASS, Firestore emulator rules, 11 tests |
| 2026-05-09 | `git diff --check` | PASS |
| 2026-05-09 | `./scripts/test.sh` | PASS, unit suites + 26 AppUITests, xcresult `Test-moodit-2026.05.09_01-16-48-+0900.xcresult` |
| 2026-05-09 | `xcodebuild ... -only-testing:AppUITests/ActionSurfaceSmokeTests/testCommerceWalletAndPayoutSurfaces` | PASS, 4 coin package actions and monthly/yearly Pro actions verified, xcresult `Test-moodit-2026.05.09_01-30-13-+0900.xcresult` |
| 2026-05-09 | `./scripts/test.sh` | PASS after StoreKit action fallback, unit suites + 26 AppUITests, xcresult `Test-moodit-2026.05.09_01-33-17-+0900.xcresult` |
| 2026-05-09 | `./scripts/test.sh` | PASS after screen/action definition alignment, unit suites + 26 AppUITests, xcresult `Test-moodit-2026.05.09_01-56-02-+0900.xcresult` |
| 2026-05-09 | `xcodebuild ... -only-testing:FilterEngineTests/PhotoFilterRendererTests` | PASS, editor reference preview render path verified, xcresult `Test-moodit-2026.05.09_03-12-31-+0900.xcresult` |
| 2026-05-09 | `xcodebuild ... -only-testing:ModelsTests/FilterManifestTests` | PASS, `signatureSampleURL` model decode verified, xcresult `Test-moodit-2026.05.09_03-27-46-+0900.xcresult` |
| 2026-05-09 | `xcodebuild ... -only-testing:AppUITests/ActionSurfaceSmokeTests/testMakerEditorAndUploadSurfaces -only-testing:AppUITests/ActionSurfaceSmokeTests/testMarketplaceSupportPermissionAndPreviewSurfaces` | PASS, editor reference controls, upload signature controls, and detail sample gallery verified, xcresult `Test-moodit-2026.05.09_03-29-09-+0900.xcresult` |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after #55/#57 |
| 2026-05-09 | `npm run build` in `functions` + `node --test test/getFilterDetail.test.mjs` | PASS, Cloud Function detail response includes `signatureSampleURL` |
| 2026-05-09 | `npm --prefix functions test` | PASS after filter detail metadata expansion, 57 tests |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after QA doc update/current issue batch |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after P0/P1 issue batch (#185/#186/#187/#188/#189/#190/#192/#207/#203) |
| 2026-05-09 | `xcodebuild ... -only-testing:FilterEngineTests/CubeLUTParserTests -only-testing:MarketplaceTests/SocialRepositoriesTests test` | PASS, LUT size cap and self-block guard covered |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after P0 state/account deletion batch (#140/#147/#227) |
| 2026-05-09 | `npm --prefix functions test` | PASS after backend P0 security batch (#142/#144/#137), 61 tests |
| 2026-05-09 | `npm --prefix functions run test:rules` | PASS after wallet/pro/refund rules hardening (#136), 12 tests |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FirebaseAppCheck iOS provider wiring (#137) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after notifications inbox QA batch (#216/#217/#218/#239/#240/#243) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after saved/favorites/wallet reconcile batch (#219/#220/#249) |
| 2026-05-09 | `npm --prefix functions run test:rules` | PASS after savedFilters/favorites owner rules, 13 tests |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after notification settings debounce/system permission batch (#247/#248) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after foreground push preference gate (#223) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FCM token auth retry (#222) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Pro included paid-filter download path (#253) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after upload cover cancel dialog fix (#254) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after signed filter package download wiring (#252) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after data export listener wiring (#250) |
| 2026-05-09 | `npm --prefix functions run test:rules` | PASS after exportRequests owner create/read rules (#250), 14 tests |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after makerDrafts listener/write wiring (#251) |
| 2026-05-09 | `npm --prefix functions run test:rules` | PASS after makerDrafts owner rules (#251), 15 tests |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after DeepLinkDestination full AppRoute coverage (#246) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after rating form default/validation fix (#245) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after MetalPreviewRenderer texture race/cache flush fix (#196/#197) |
| 2026-05-09 | `xcodebuild ... -only-testing:FilterEngineTests test` | PASS after Camera/FilterEngine residual safety fixes (#210) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after ReviewCompose validation/stars/error-state fix (#244 partial, #153/#155) |
| 2026-05-09 | `npm --prefix functions test` | PASS, 81 tests after `reviewImageUploadInit` R2 presigned upload contract and review `photoUrl` detail response coverage (#244) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after ReviewCompose image upload wiring and ReviewsList attachment rendering (#244) |
| 2026-05-09 | `npm --prefix functions run build` + `node --test functions/test/identity.test.mjs` | PASS, 17 identity tests after `profileAvatarUploadInit` and profile avatar URL/object-key validation (#183) |
| 2026-05-09 | `npm --prefix functions test` | PASS, 85 tests after profile avatar upload contract was added (#183) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after profile avatar R2 upload and `avatarURL` rendering wiring (#183) |
| 2026-05-09 | `xcodebuild ... -only-testing:AppUITests/ActionSurfaceSmokeTests -only-testing:AppUITests/PhaseAE2ETests -only-testing:AppUITests/PhaseDE2ETests -only-testing:AppUITests/P0CoreActionTests` | PASS, 32 AppUITests after UI test helper consolidation and action contract refresh (#184) |
| 2026-05-09 | `xcodebuild ... -only-testing:AppTests/UniversalLinkParserTests` | PASS, 21 tests after percent-encoded route slug coverage (#184) |
| 2026-05-09 | `npm --prefix functions test` | PASS, 93 tests after idempotency cache implementation, Apple cert timeout coverage, and paid filter entitlement/Pro gating in `getFilterDetail` (#182) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after #162 screen/action telemetry wiring |
| 2026-05-09 | `xcodebuild ... -only-testing:AppTests/TelemetryTests test` | PASS, 3 tests after #162 telemetry parameter sanitizer coverage |
| 2026-05-09 | `rg "makerFilters\\[[^\\]]+\\]" Sources/App/MooditStore.swift` + `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS, no remaining `makerFilters[index]` mutation/access pattern after immutable map update (#164) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after moving tab NavigationStack/path ownership into `RootShell` and disabling nested tab stacks where embedded (#161) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FilterDetail sample gallery lightbox with paging, pinch/double-tap zoom, swipe-down dismiss, close control, and counter (#139) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after EditProfile username debounce availability check, status UI, VoiceOver announcement, and save gating (#145) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after AccountDeletion policy acknowledgement, username/email confirmation, destructive alert copy, and explicit data-retention warnings (#143) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after own/other Profile CTA alignment, other-profile follow/share/menu actions, profile a11y labels, and report/block routing (#141) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after card/row VoiceOver grouping pass for filter tiles, marketplace cards, notifications, collections, and follow rows (#148) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` + `xcodebuild ... -only-testing:FilterEngineTests/PhotoFilterRendererTests/testAspectRatioUsesPortraitOrientationWhenHeightIsGreaterThanWidth test` | PASS after 4:5 camera aspect support, live aspect chip strip, and animated viewfinder mask update (#149) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after modal dismiss policy alignment for camera covers, edit profile, editor, and upload workflow (#157) |
| 2026-05-09 | `npm --prefix functions test` | PASS, 75 tests after Firestore rate limit and price tier backend hardening (#174/#172) |
| 2026-05-09 | `npm --prefix functions test` + `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS, 79 Functions tests and iOS build after IAP duplicate/refund order validation fixes (#169/#168) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after StoreKit finish/restore backend sync fix (#150/#152) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after profile save task cancellation/generation guard (#156) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after removing AppStorage-backed auth state in favor of MooditStore auth listener (#158) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FilterDetail bottom CTA `safeAreaInset` layout fix (#114) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after camera overlay contrast/hit-area polish and tab bar Dynamic Type fallback (#86/#87/#88/#89/#90/#94/#96/#108/#110) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after camera top-control state labels and overlay readability polish (#78/#81/#82/#83/#85) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after camera preview unavailable empty state and import fallback (#135) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after caching workflow date/time formatters (#179) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FilterDetail share/description/tag interaction polish (#116/#138/#146) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FilterDetail like/follow affordance polish (#115/#117) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after camera top chrome, timer state, AUTO label, and swipe hint placement polish (#72/#73/#76/#84/#91) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after camera timer 5s option and explicit countdown cancel controls (#151) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after removing duplicate bottom flip and compressing camera bottom stack (#100/#103) |
| 2026-05-09 | `xcodebuild ... -only-testing:AppUITests/PhaseAE2ETests/testCameraHudAndPhotoImportEntry test` | PASS after compact zoom controls; `camera.zoom.0.5`, `camera.zoom.3.0`, and `camera.openLibrary` selectors still work |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after EmailLogin password visibility toggle and strength meter (#177) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FilterDownload success/failure toast feedback (#171) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after HelpCenter FAQ search/category filtering (#159) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Saved search/sort/category/edit bulk-delete controls (#170) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Search cancel transition and Dynamic Type chip/maker layout polish (#132/#131) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Search 250ms debounced live results and result pull-to-refresh (#70/#75) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Saved empty-state pull-to-refresh and duplicate refresh guard (#74) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Profile and FilterDetail pull-to-refresh wiring (#77/#80) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after follower/following list pull-to-refresh wiring (#79) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Notifications pull-to-refresh and #71 refresh coverage completion |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FMToast Reduce Motion, VoiceOver announcement, swipe dismiss, and haptic feedback polish (#93) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Notifications unread affordance and mark-all-read action (#111) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after shared locale-aware compact counter formatting for marketplace/detail/profile/social counts (#95) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after marketplace filter tile title/meta truncation and stable info-block sizing (#120) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after marketplace tile image skeleton and next-cover prefetch wiring (#119) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Marketplace category strip scroll-to-selected with Reduce Motion support (#165) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Login social/email button hierarchy and shared 50pt sizing polish (#121) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Profile stat link hit-area, chevron, press feedback, and accessibility hints (#118) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Onboarding visible page count, final CTA hierarchy, and Reduce Motion animation polish (#113) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Settings account/notification/permission/data/info/danger hierarchy polish (#112) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FMTabBar badge API and RootShell notification/upload badge wiring (#109) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after shared FMTextField input hints and form-specific keyboard/submit/autocapitalization polish (#107) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Camera overlay text Dynamic Type token migration and xLarge cap (#106) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Upload workflow autosave trigger and cancel-confirm coverage (#105) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Upload pending step indicator and progress accessibility coverage (#104) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after EmptyState recovery CTA wiring for marketplace, saved downloads, and filter detail not-found (#102) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after PermissionDenied settings recovery and foreground permission recheck wiring (#101) |
| 2026-05-09 | `xcodebuild ... -only-testing:AppUITests/DynamicTypeRegressionTests/testCoreRoutesAtAccessibility5ExposePrimaryActionsInsideViewport test` | PASS after `-ui-dynamic-type accessibility5` launch override and Search/Profile Firebase-disabled crash guards (#99) |
| 2026-05-09 | `xcodebuild ... -only-testing:AppUITests/PhaseAE2ETests/testCameraHudAndPhotoImportEntry test` | PASS after camera library button recent-photo thumbnail wiring; `camera.openLibrary` still opens PhotoImport (#98/#134) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after camera library thumbnail PhotoKit loader (#98/#134) |
| 2026-05-09 | `rg "AsyncImage" Sources/App Sources/DesignSystem -n` + `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS, direct `AsyncImage` usage is centralized in `FMRemoteImage` (#97) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after Reduced Motion propagation across remaining app and DesignSystem animation surfaces (#69) |
| 2026-05-09 | `xcodebuild ... -only-testing:AppTests/FMColorsContrastTests test` | PASS, light/dark token contrast regression coverage added (#92) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FilterDetail before/after comparison handle polish (#122) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after editor parameter sliders moved to precision-capable FMSlider (#123) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after LUT import helper/error recovery copy (#124) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FilterEditor cancel confirmation moved to 3-way action sheet (#125) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after WalletTopup package row feedback/layout polish (#126) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after wallet transaction empty state and filter chip polish (#127) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` | PASS after FilterRejected recovery actions and policy copy (#128) |
| 2026-05-09 | `xcodebuild ... -destination 'generic/platform=iOS Simulator' ... build` + `xcodebuild ... -only-testing:AppTests/UniversalLinkParserTests test` | PASS after invalid deep-link fallback landing and telemetry wiring (#129) |
| 2026-05-09 | `xcodebuild ... -only-testing:AppTests/SystemImageAccessibilityAuditTests test` | PASS, 183 `Image(systemName:)` calls are documented and guarded by the system image accessibility audit (#130/#244) |

## Remaining Manual QA Gates

| Gate | Why automated QA is insufficient | Required setup | Pass evidence |
|---|---|---|---|
| Apple Sign-In | Native auth sheet and real credential flow cannot be fully asserted by simulator smoke tests | Apple developer capability, signed build; see `EXTERNAL_SETUP.md` §3, §5 | Tap `auth.apple`, complete Apple sheet, app reaches authenticated root with user profile populated |
| Google Sign-In | `Sources/App/Resources/GoogleService-Info.plist` exists, but external account picker and Firebase credential exchange still require interactive account QA | Test Google account, Firebase Auth provider enabled; see `EXTERNAL_SETUP.md` §6, §7 | Tap `auth.google`, complete Google account picker, Firebase user appears and app reaches authenticated root |
| StoreKit purchase/restore | UI-test mode now verifies the 4 coin package buttons and monthly/yearly Pro buttons, but product availability, real purchase, restore, and cancellation still require StoreKit sandbox or a valid local `.storekit` config | Sandbox tester plus App Store Connect products, or add local `.storekit`; see `EXTERNAL_SETUP.md` Phase 6 | 100/550/1200/3000 coin purchase updates wallet, monthly/yearly Pro activates Pro state, restore recovers entitlement |
| Camera capture | Simulator cannot validate real camera session, photo quality, focus, capture latency | Physical iPhone | `camera.shutter` captures a photo, preview opens, focus/flip/flash/timer controls remain responsive |
| Camera shutter long-press | CameraScreen now distinguishes ready/processing/disabled/burst shutter states with stroke/fill/progress changes, maps long-press to a 3-shot burst sequence, adds heavy + selection haptics, and exposes VoiceOver label/value/hint plus a "연속 촬영" custom action. | Physical iPhone or interactive simulator with camera route | Tap shutter for a single capture, long-press for burst, verify the final burst frame opens preview, observe haptics/state changes, and test VoiceOver plus Reduce Motion. |
| Camera filter swipe | CameraScreen now applies a 50pt drag threshold, velocity/predicted-end inertia for 2~3 filter jumps, first/last edge resistance without wrap-around, thumbnail strip sync, Reduce Motion-aware snap, and VoiceOver adjustable previous/next actions on the current filter pill. | Physical iPhone or interactive simulator with camera route | Swipe slowly under/over 50pt, fast-swipe across 2~3 filters, drag beyond both edges, enable Reduce Motion, and verify VoiceOver reads "필터명, 강도 N퍼센트" while adjustable flick changes filters. |
| Camera lifecycle safety | Camera countdown tasks are now cancellable and cleared on background/inactive/disappear; cancelled sleeps no longer continue into capture. CameraSession.capturePhoto now returns `sessionUnavailable` instead of restarting a stopped AVCaptureSession. | Device QA should start a timer countdown, background the app, return to foreground, and verify no background capture occurs. Then take a normal photo after the session restarts. |
| Camera preview renderer memory/race | MetalPreviewRenderer now serializes LUT texture builds, discards stale texture generations, clears the current frame on stop, and flushes `CVMetalTextureCache` periodically and on stop. Memory plateau and rapid filter switching still need device/Instruments confirmation. | Physical iPhone, Instruments Metal System Trace/Allocations | Rapidly switch filters 10+ times under 1s and verify the latest filter applies; run camera preview for 5 minutes and verify texture/cache allocations remain bounded; background/foreground and confirm preview resumes normally |
| Photo save/share | Actual Photos permission and share sheet destinations are OS/external surfaces | Physical iPhone with Photos access | `preview.save` writes to Photos and `preview.share` opens a working destination |
| Push notification | APNs token, FCM registration, notification tap routing require device/APNs path | Physical iPhone, Firebase/APNs config; see `EXTERNAL_SETUP.md` §11 | Console logs FCM token persistence, Firebase Console test notification arrives, tap routes to expected app screen |
| Push first-login registration | FCM token timing depends on Firebase/APNs and can arrive before Auth is ready | Fresh install on physical iPhone, Firebase/APNs config | After first login, `/users/{uid}/devices/{deviceId}` exists even if token was issued pre-login |
| App Settings links | `UIApplication.openSettingsURLString` leaves the app and needs OS-level validation | Device or interactive simulator run | Permission/settings buttons leave app to moodit Settings page and returning preserves app state |
| PhotosPicker reference/signature import | Automated QA verifies entry points only; OS Photos picker, permission prompt, and real image normalization require interactive QA | Simulator with seeded Photos library or physical iPhone with Photos access | `editor.reference.photo.pick` updates `editor.preview`; `upload.signature.photo.pick` updates `upload.signature.preview`; clear/sample fallback works |
| PhotoImport single-selection and limited library | PhotoImport now keeps a single-image `PhotosPickerItem?` policy, exposes selected-preview accessibility, shows the iOS Limited Photos notice with visible image count, and routes "사진 더 선택" to the limited-library management picker. OS picker selection remains interactive QA. | Simulator with a seeded Photos library or physical iPhone; set Photos access to Limited and 0/1+ allowed images | `photo.import.cell.tap` opens single image selection, selected preview appears, `photo.import.next` routes to PhotoEdit, limited notice and `photo.import.limited.manage` appear when access is Limited |
| PhotoEdit undo/original compare | PhotoEdit now has toolbar undo/redo, reset confirmation, preview long-press original comparison, and VoiceOver custom actions for undo/redo/reset while preserving filter/intensity/save/share actions. | Simulator with seeded Photos library or imported sample image | Change filter and intensity, verify undo/redo stacks, confirm reset/cancel behavior, hold preview to see original, and test Reduce Motion plus VoiceOver actions. |
| Builtin filter library grouping/Pro lock | BuiltinFilterLibrary now has search, category chips, sticky category sections, free/Pro price labels, lock badges, locked-card VoiceOver value, and a Pro guidance sheet for locked filters. | Simulator/device with at least one seeded paid filter or temporary test seed where `priceCoins > 0` and `isProActive == false` | `builtin.search` filters results, `builtin.category.*` narrows sections, locked cards show Pro badge/value and `builtin.filter.locked.pro` routes to Pro subscription |
| Profile avatar import | EditProfile opens PhotosPicker, stores a normalized local avatar preview, uploads the JPEG through `profileAvatarUploadInit` + R2 presigned PUT on save, and persists `avatarURL`/`photoURL`/`avatarObjectKey` to `/users/{uid}`. Profile/settings avatars render the remote URL with initials fallback. OS picker and real image selection still need interactive QA. | Simulator with seeded Photos library or physical iPhone with Photos access, Firebase/R2 configured with `R2_PUBLIC_BASE_URL` | `profile.edit.avatar.change` updates the avatar preview; save uploads the object, `/users/{uid}` includes `avatarURL`, and the avatar remains visible after app restart/second-device reload |
| Profile grid routing | Profile grid items now carry separate display title, route ID, and kind. My filter/saved filter tiles route by filter UUID, while capture tiles route to a capture detail placeholder instead of treating a capture document ID as a filter ID. | Firebase QA should seed `myFilters`, `savedFilters`, and `captures`, tap each segment tile, and verify filter detail or capture detail navigation without not-found caused by titles/capture IDs. |
| Capture metadata | Camera capture save now keeps the existing Photos-library save path and additionally writes capture metadata to `/users/{uid}/captures` under owner-scoped rules. Profile captures can populate from this listener without uploading the original photo. | Device/Firebase QA should capture and save a photo, confirm a captures document appears with filter/aspect/intensity metadata, and verify Profile → Captures updates. |
| Signature sample R2 upload | UI can select a signature sample and backend preserves `signatureSampleURL`, but real image upload/final URL wiring is not yet connected in the SwiftUI submit flow | Backend endpoint or storage workflow for signature image upload | Submitted filter detail contains a real `signatureSampleURL`; detail gallery first tile displays the uploaded image |

## Open QA Notes

| Area | Note | Follow-up |
|---|---|---|
| `filter.detail.tag.*` | `ActionSurfaceSmokeTests` verifies the stable `filter.detail.tags` container. Individual lazy tag chip selectors are still left for manual QA because SwiftUI accessibility flattening was unstable in the simulator. | Verify manual tag navigation through `QA_TEST_PLAN.md` §5.9, then add a targeted chip tap test if selector stability improves. |
| `filter.detail.sample.reference.*` | Automated QA verifies the gallery and portrait reference tile. FilterDetail now opens a full-screen sample lightbox with page swipe, pinch/double-tap zoom, swipe-down dismiss, close control, and counter. Full visual correctness still needs manual inspection because rendered LUT output is image-based. | Verify `QA_TEST_PLAN.md` §5.10~§5.12a on simulator/device for all four reference samples, zoom/dismiss gestures, and cache re-entry behavior. |
| `filter.detail.like` | Like CTA is now stateful for real filters and mock detail previews, but production favorite/like semantics still depend on the final backend distinction between saved/favorite/like. | Confirm whether detail heart should write favorites, likes, or both before release analytics/backfill. |
| `social.followers` / `social.following` | Follow lists now read Firestore root `follows` edges and user profile docs, with local fallback for unauthenticated QA. | Seed Firebase with follow edges and verify row population, empty states, follow toggle, and follower/following counter triggers. |
| `market.header.coinBalance` | Marketplace exposes wallet navigation through the balance pill. Product availability is still covered by StoreKit manual QA. | Verify logged-in wallet listener updates the pill and tapping opens `WalletScreen`. |
| Profile username copy | Edit profile/settings/data export copy now uses "유저네임" instead of "핸들"; existing accessibility IDs remain `profile.edit.handle*` for selector stability. | Manual Korean copy pass before App Store screenshots. |
| EditProfile username availability | EditProfile now normalizes username input, validates 3~20 lowercase ASCII/number/underscore format, debounces `/handles/{username}` availability checks by 400ms, announces status changes to VoiceOver, and blocks save unless the username is available. The final write still goes through the authoritative `setHandle` callable. | Firebase QA should seed `/handles/<name>` for another UID and verify unavailable state, then test own existing username, reserved usernames, invalid characters, and offline/permission failure messaging. |
| Profile share link handle resolution | Universal links and custom scheme maker links now route to `otherProfileHandle`, resolve `/handles/{handle}` to uid, and only then render `ProfileScreen(otherUid:)`. `/handles` is public-read/client-write-blocked in rules. | Firebase QA should share `https://moodit.app/u/<handle>`, open it on another install, and verify the resolved uid profile loads; unknown handles should show a not-found message. |
| Deferred deep links | RootShell now defers incoming deep links until onboarding is complete and, for auth-required routes, until login is complete. Deferred and flushed routes emit telemetry and then present through the existing pending route sheet. | QA should open a universal link during first-run onboarding, finish onboarding, and verify the route opens once; repeat with an auth-required route while logged out and verify it opens after login. |
| Invalid deep-link fallback | RootShell now routes unparseable incoming URLs and UI-test deep-link URLs to `UniversalLinkLandingScreen` instead of silently ignoring them, and emits `deep_link_failed` telemetry with reason/scheme/host/path. UniversalLinkLanding now shows an actionable fallback for invalid, expired, or deleted links with market/search recovery actions instead of a mock first-filter card. | Manual QA should open malformed moodit links, unknown hosts, expired invite links, and deleted filter links, then verify the fallback sheet and recovery buttons. Valid links should still route directly. |
| Other profile loading | Other-user profile screens now show explicit not-found/error states with retry instead of leaving `...` placeholders, normalize displayed handles to a single `@` prefix, and skip self-profile listeners for otherUid routes. | QA should open an unknown uid and a temporarily offline profile route, verify the message/retry behavior, then open an existing user and confirm the loaded handle/share URL are normalized. |
| Profile own/other CTA alignment | Own and other profiles now share the same CTA row position: own profile shows edit + share, other profile shows follow/following + share. Other profile toolbar exposes share/report/block through `profile.other.menu`, and block/follow write the same root `blocks`/`follows` edge patterns used elsewhere. Header VoiceOver labels distinguish "내 프로필" and "메이커 프로필". | Firebase QA should verify follow edge creation/deletion, block edge creation, report routing, share sheet, and Dynamic Type layout on both own and other profiles. |
| Card/row VoiceOver grouping | FMFilterTile now exposes one combined label with title/maker/download/price plus a detail hint. Marketplace featured/collection cards, favorites collection cards, notification rows, and follower/following profile areas now provide combined labels/hints while keeping follow CTAs as separate controls. Decorative chevrons/badges are hidden where the parent label owns the meaning. | Manual VoiceOver QA should flick through Marketplace, Search results, Saved, Favorites collections, Notifications, and Followers/Following to confirm each card/row is read once and secondary CTAs remain reachable. |
| Camera aspect chips | Live Camera now uses an in-place horizontal chip strip for 1:1, 4:5, 4:3, and 16:9 instead of a compact menu. Selecting a chip updates `store.cameraAspectRatio`, applies the controller crop ratio immediately, and animates the viewfinder mask for 200ms unless Reduce Motion is enabled. The filter renderer and aspect configuration screen now support 4:5. | Device QA should switch all four ratios over bright/dark scenes, verify guide/captured JPEG crop parity, haptic feedback, VoiceOver label/value/hint, and no overlap with the top controls on the smallest supported device. |
| Modal dismiss policy | Camera full-screen covers now disable interactive dismiss. EditProfile uses a top-leading close button and blocks swipe dismiss only while changes are unsaved, with discard/continue confirmation. FilterEditor and Upload Cover/Tags/Submit disable interactive dismiss, use top-leading close controls, and route cancellation through save/discard/continue confirmation dialogs. `MODAL_PATTERNS.md` now records the policy table. | Manual QA should attempt swipe-down on camera, editor, upload, and edited profile sheets, then verify simple system sheets such as share/Safari/PhotoPicker still use system dismiss behavior. |
| Filter detail download | Detail CTA now owns and cancels its download task and only transitions to completed after `store.download` succeeds. | Manual offline/Firebase permission-denied QA should verify the failure alert and no stale completed CTA after dismissal. |
| Filter package download | FilterDownloadProgress now uses `signedDownloadURL` for UUID-based filters, streams bytes to `Application Support/moodit/downloaded-packages/<filterId>.fmpkg`, and only writes savedFilters after the package fetch succeeds. | Firebase/R2 QA should download a real large and small package, verify local file existence, progress behavior, retry on network failure, and offline apply wiring in the renderer path. |
| FMPKG builder hardening | FmpkgBuilder now rejects traversal/absolute/backslash LUT filenames and propagates manifest JSON encoding failures instead of returning empty Data. | Unit tests cover malicious LUT filenames; future ZIP extraction must keep entry paths inside the destination directory. |
| Metal preview renderer safety | `apply(configuration:)` no longer builds LUT textures based on stale lock-outside state; texture creation is serialized and guarded by a generation check. `CVMetalTextureCache` is flushed every 60 rendered frames and when camera preview stops. | Device QA should pair rapid filter switching with Instruments memory/Metal trace because simulator build cannot prove real camera texture cache behavior. |
| Camera/FilterEngine residual safety | PhotoFilterRenderer now shares a Metal-backed CIContext when available and drains row-level autorelease pools during CPU pixel loops; CameraPreviewController guards overlapping start/stop calls with a lifecycle generation; LUT public APIs reject invalid sizes; FMPKG verifier rejects 1D LUT packages; capture preview no longer sends raw corrupted bytes to share sheet. | Device/Instruments QA should verify 12MP capture peak memory, rapid start/stop while entering background, and share failure copy for intentionally corrupted image data. |
| Reviews list listener | Reviews Firestore listener is removed on disappear and now decodes `stars`, `helpfulCount`, and `isVerifiedDownload`. | Firebase seeded review QA should verify live updates after navigating away/back. |
| Review compose validation | ReviewCompose starts with an empty body, clamps text input to 280 chars, requires 5+ trimmed chars and a 1~5 star rating, sends `stars` with the Firestore review payload, and surfaces inline errors. Photo attachment now uploads through `reviewImageUploadInit` + R2 presigned PUT, stores `photoUrl`/`photoObjectKey` on `/filters/{filterId}/reviews/{uid}`, and ReviewsList renders `social.review.image` through `FMRemoteImage`. | Firebase/R2 QA should configure `R2_PUBLIC_BASE_URL`, submit text+stars+photo, verify the R2 object exists, review doc includes `photoUrl`, and the image appears after navigating away/back. |
| Reviews mini card | ReviewsListScreen now resolves the filter title, maker, rating/download metadata, and cover image from local store or `filters/{filterId}` instead of showing a generic UUID fallback. | Push/deep-link QA should open a UUID filter's review list directly and verify the mini card matches the source notification/detail screen. |
| Review helpful toggle | Review helpful state now syncs through `/users/{uid}/reviewHelpful` and updates `filters/{filterId}/reviews/{reviewId}.helpfulCount` in a transaction with optimistic rollback. | Firebase QA should verify same-user re-entry preserves the filled thumb state and double tap cannot create duplicate helpful edges. |
| Filter detail mapping | getFilterDetail now returns `description`; iOS maps it into FilterDetailScreen, derives placeholder color from the response category, and routes purchase/download/reviews with the filter document ID instead of the display title. Sample preview cache write failures are logged. | Firebase QA should open a real UUID filter detail, verify the description is not generic copy, then tap purchase/download/reviews and confirm downstream screens receive the UUID. |
| Filter detail loader state | FilterDetailLoaderScreen now uses phase as the single render source. Local UUID filters render immediately, then production continues to `getFilterDetail` so backend samples/reviews/counters replace the local placeholder without a blank loading flash. | QA should open a locally seeded filter with backend detail data and verify there is no detail flicker while remote samples/review counts eventually replace fallback content. |
| Filter detail samples/review counters | FilterDetail now consumes `getFilterDetail.samples[]` before falling back to signature/reference samples. Firestore triggers also maintain root `reviewCount`/`ratingAvg`, `sampleCount`, and `likeCount` from reviews/samples/likes subcollections. | Firebase QA should seed `filters/{id}/samples`, `reviews`, and `likes`, then verify detail gallery shows remote samples, preview reviews show real authors/body, and root counters update after create/update/delete. |
| Refund request flow | OrdersHistory now exposes a refund action for every order plus the generic support action. RefundRequest accepts a direct/manual order ID or a read-only order ID prefilled from order history, caps the reason at 2000 chars, and dismisses after a successful callable submit. | Firebase QA should seed purchase ledger rows, open `orders.refund_request.<orderId>`, verify the prefilled ID, submit a valid reason, and confirm the Cloud Function receives trimmed `orderId`/`reason`. |
| For You feed | ForYouFeedScreen now builds hero, rail, reason text, and maker spotlight from `MooditStore` filter data instead of hardcoded mock content. Hero/rail routes use filter UUIDs and the hero bookmark calls `MooditStore.toggleFavorite`. | Firebase QA should seed approved filters, verify hero/rail detail navigation by UUID, favorite persistence, and empty-state behavior when no filters are available. |
| Following feed | FollowingFeedScreen now listens to root `follows` edges, loads approved filters from followed makers, uses filter UUIDs for detail/review routes, persists likes/hides in `/users/{uid}/feedActions`, and saves posts through favorites. | Firebase QA should follow a maker with approved filters, verify new-filter card/post rows, like/hide persistence, favorite sync, and true empty state for users with no followed activity. |
| Block list and review author block | BlockListScreen now listens to root `blocks` edges scoped by `actorUid`, shows load/unblock failures with retry/error copy, and deletes by UID edge ID. ReviewsListScreen writes `blocks/{actorUid}_{targetUid}` from `social.review.more.block`, stores target handle/display name as metadata, and filters blocked author reviews by UID. | Firebase QA should block a review author, verify the block appears immediately in Settings → Block List, relaunch and confirm the author remains hidden, then unblock and verify the row is removed. |
| Review row ownership/actions | ReviewsListScreen now distinguishes own rows with an "내 리뷰" visual/accessibility label, exposes own edit/delete and other-user reply/report/block actions from both the more menu and row swipe actions, and keeps reply composition flat by pre-filling the target handle instead of nesting beyond the maker reply row. | Firebase QA should seed own/other reviews, verify action sets differ by authorUid, edit/delete only the own review, reply to another review via mention prefill, and confirm block/report routing. |
| Report target routing | AppRoute.reportForm now carries a typed target. ReportFormScreen shows a read-only target summary instead of a manual filter ID input, and submits filter/review/user reports to separate callable paths. Review more → report now passes review ID, filter ID, and author UID. | Firebase QA should report a filter, a review, and a user, then verify the corresponding report document and counter are written without requiring ID entry from the user. |
| Notification follow action | Follow notification action now writes a root `follows/{actor}_{target}` edge before marking the notification read. | Seed follow notification and verify count trigger/list update in Firebase. |
| Root wallet/profile subscription | RootShell now starts the wallet/profile/auth listener on cold start, and MooditStore listener callbacks re-enter MainActor before publishing state. | Device/Firebase QA should verify cold-start balance, Pro status, profile handle onboarding, and notification preference state. |
| Foreground listener recovery | RootShell now observes `scenePhase == .active`, logs `app_resumed`, forces an Auth token refresh, reattaches user-scoped listeners, retries push device registration, and reloads filters. | Device QA should background the app, mutate wallet/profile data from another device, return to foreground, and verify balance/profile/preference state refreshes without relaunch. |
| Account deletion | Account deletion now shows explicit data-impact copy for login disablement, profile/follow/upload filter handling, local downloaded files, purchase/retention records, coin/payout/refund handling, and the current lack of a 30-day recovery grace period. Submit requires policy acknowledgement plus exact username/email confirmation before the destructive alert and `deleteAccount` callable. | Manual Firebase QA should verify `users/{uid}` soft-delete fields, Auth user deletion/sign-out, failure alert behavior, and that submit remains disabled until both acknowledgement and exact confirmation are present. |
| Data export requests | DataExportScreen now reads `/users/{uid}/exportRequests` through a listener, maps backend statuses/download URLs into the history list, and rules allow owner create/read only. | Backend QA should update an export request to `ready` with `downloadURL` and verify the button appears across app restart/second device. |
| Maker drafts list | Maker draft metadata now syncs through `/users/{uid}/makerDrafts`, and MyFilters repopulates from a listener after restart/second-device login. Direct signature sample photo bytes are not stored in this metadata doc. | Firebase QA should save multiple drafts, force-quit/relogin, verify MyFilters restoration, then confirm image binary persistence once Storage upload wiring is added. |
| Active editor draft autosave | The in-progress editor draft now persists per uid to local UserDefaults and `/users/{uid}/editorDrafts/current`, restoring after cold start/relogin and syncing metadata across devices. Logout clears in-memory state but keeps the saved draft for the same uid. | QA should edit name/parameters/tags, force-quit, relaunch, and verify the editor restores. Then login on a second device and verify the remote current draft appears; direct signature photo bytes remain local-only until image storage wiring. |
| Editor preview responsiveness | Editor parameter preview now uses a 16ms render delay, downscales user reference images to an 800px preview input, renders directly to `CGImage` for preview, and only shows the spinner during the first render so slider updates do not get covered by a central loader. | Manual QA should drag all parameter sliders continuously and verify the preview updates without the old 250ms wait or center ProgressView. Full GPU/MTKView preview remains a later optimization candidate. |
| Editor parameter precision | EditorParameters now uses FMSlider for each parameter instead of native Slider. FMSlider supports long-press precision mode, 0 snap with "원본" marker, double-tap reset to 0, floating value labels, 5% VoiceOver adjustable actions, Reduce Motion-aware thumb animation, and step haptics. | Manual QA should long-press a parameter slider, drag slowly around 0, verify the "정밀" label and 0 snap, double-tap reset, then confirm camera intensity slider behavior remains acceptable because it also uses FMSlider. |
| LUT import guidance | EditorLUTImport now shows supported `.cube` 1D/3D format, recommended 33x33x33 size, max 64x64x64, and likely failure causes. Import failures use a clearer unsupported-LUT alert with parser-specific cause text and a "다시 선택" CTA that reopens the document picker. | Manual QA should import valid `.cube`, malformed `.cube`, oversized LUT, and non-LUT files from the picker and verify the validation card, preview, alert copy, and retry path. |
| Editor cancel confirmation | FilterEditor cancel now uses the standard confirmation dialog with three explicit choices: temporary save and dismiss, destructive discard and dismiss, or continue writing. The dialog copy explains that saved drafts can be resumed and discarded changes are removed. Upload cancel dialogs already expose equivalent 3-way behavior. | Manual QA should open the editor cancel dialog, verify destructive styling on discard, cancel keeps the editor open, and save/discard leave the editor with the expected draft state. |
| App Check | Callable Cloud Functions now enforce App Check and iOS registers AppCheckDebugProviderFactory in DEBUG / AppAttestProviderFactory in release. | Register debug tokens in Firebase Console for dev devices/CI before exercising callable flows. |
| Backend rate limit and price tiers | Cloud Functions now use Firestore-backed rate limit counters for purchase, IAP credit, refund request, and report callables; filter priceCoins is validated against allowed tiers at submit, approval, and purchase. | Production QA should monitor `_ratelimit` write volume/cost and verify invalid tier migrations fail before publication or purchase. |
| IAP duplicate and refund validation | Duplicate `creditCoinsFromIAP` calls now return `creditedAmount: 0`, and iOS top-up success reconciles from the callable balance when available. Refund requests must reference an existing owner walletLedger purchase/topup entry, duplicate requests reuse the order ID as the document ID and are rejected, and HTML tags are stripped from the reason. | StoreKit/Firebase QA should replay the same transaction ID and verify no double top-up toast/balance; then request refund for valid, missing, duplicate, and non-refundable ledger IDs. |
| StoreKit finish/restore safety | StoreKitManager now finishes verified transactions only after backend sync succeeds, leaves failed sync transactions unfinished for StoreKit retry, and restore replays current entitlements through the same backend bridge before marking products restored. | StoreKit sandbox QA should simulate backend/network failure, verify transaction retry, then run restore after reinstall and confirm coin/pro state syncs without duplicate credit. |
| Profile save race | MooditStore now owns the active profile-save Task, cancels superseded saves, invalidates in-flight work on user-scope reset, and applies completion/error messages only for the latest generation. | Manual QA should tap profile save repeatedly under slow network and verify only the latest result message persists after dismissal/re-entry. |
| Auth state source | `isAuthenticated` is now published by MooditStore from Firebase Auth state changes, with UI-test launch args read directly into the store instead of UserDefaults. RootShell, profile, search, review, login, email login, and settings no longer own AppStorage auth flags. | Firebase QA should revoke/expire a token, relaunch, and verify auth-required routes defer to Login immediately; sign-out should clear user-scoped store state without stale AppStorage. |
| Filter detail CTA layout | FilterDetail now hosts the bottom CTA through `safeAreaInset(edge: .bottom)` and removed the hardcoded review-section bottom padding. ScrollView reserves the CTA area dynamically instead of letting final review content sit under the fixed bar. | Manual QA should inspect the last review on small devices and large Dynamic Type to confirm it remains fully reachable above the CTA. |
| Camera overlay readability | Camera grid lines now use higher contrast with a dark shadow, top controls expose 44pt hit areas, the filter chip no longer duplicates the intensity percent, inactive filter/zoom labels have stronger contrast, and zoom chips use consistent dimensions. FMTabBar hides visual labels at `.xxxLarge` and above while keeping accessibility labels. | Manual QA should inspect camera overlays over dark, bright, and mixed scenes, then test Dynamic Type `.xxxLarge`/accessibility sizes for tab bar icon-only fallback. Full CameraScreen text-token migration remains tracked separately by #106. |
| Camera control state clarity | Grid ON now uses accent tint/border, flash shows the current OFF/AUTO/ON label without opening the menu, swipe hints use a solid dark pill without compounded opacity, and the current filter label has a scrim/shadow to stay readable over grid/viewfinder content. | Manual QA should verify flash/grid/timer/aspect controls fit on the smallest supported device width and remain readable over bright camera frames. |
| Camera preview unavailable state | CameraPreviewController now publishes a preview-unavailable state when the session fails to start. CameraScreen shows a centered error/empty state with a photo-library import CTA instead of leaving a black viewfinder with only grid lines. | Simulator QA should verify `camera.unavailable` appears when hardware preview is unavailable; device QA should confirm normal camera preview hides the fallback. |
| Workflow date/time formatting | `workflowDateString` and `workflowTimeString` now use shared static formatter instances instead of allocating a new `DateFormatter` on every row render. | Visual QA should confirm dates still render as `yyyy. M. d. HH:mm` and times as `HH:mm` in My Filters, data export, upload review, and wallet/order surfaces. |
| Filter detail interaction polish | The share button now uses a 44pt target with label/hint, long descriptions default to 4 lines with a more/collapse toggle, and hashtag chips reserve a 44pt tap area with press scale, haptic, and search accessibility hints. | Manual QA should verify long copy expansion/collapse, tag search routing, and toolbar contrast in light/dark appearances. |
| Filter detail like/follow affordance | The maker follow button now has a 44pt target, filled inactive state, selected accessibility value, and press feedback. The bottom like CTA now shows the like count, active tint, dynamic label/value, and scale feedback. | Firebase QA should confirm follow/favorite backend semantics and rollback/toast behavior once production mutations are wired. |
| Camera top chrome and hints | Camera top controls now use darker solid overlay chrome with stronger borders, timer OFF/active states have separate icon/tint/border treatment, AUTO is presented as non-interactive status text, and swipe hints sit lower than the center of the viewfinder. The filter indicator no longer duplicates the intensity percent. | Manual device QA should verify the 260pt lower hint offset does not collide with the bottom stack on the smallest supported device and remains outside common face/subject framing. |
| Camera timer escape route | CameraTimerOption now includes 5s in addition to OFF/3s/10s. During countdown, the shutter becomes a cancel control, the overlay exposes a visible cancel button with `camera.timer.cancel`, and the final second emits light haptic feedback. | Device QA should verify 3s/5s/10s all count down correctly, shutter re-tap cancels immediately, and capture only fires after an uncancelled countdown. |
| Camera bottom stack density | The bottom stack no longer includes a dedicated zoom row, and the duplicate bottom-right camera flip button has been replaced by compact zoom controls that preserve `camera.zoom.*` selectors. The current-filter label is compressed to one row, leaving the slider as the only visible intensity percentage. | Small-device QA should verify `camera.shutter`, `camera.openLibrary`, `camera.flip`, and zoom selection remain reachable without crowding the shutter row. |
| Email login form | EmailLogin now supports password visibility toggle with `auth.password.visibility`, keeps password/new-password content types for Autofill, and shows a three-step signup strength meter plus Korean strength copy. Existing password reset flow remains available through `auth.passwordReset.send`. | Manual QA should verify Password Manager suggestions still appear for both visible and secure password modes, and that reset email failure/success states remain clear. |
| Filter download feedback | FilterDownloadProgressScreen already had determinate progress, completed next action, cancel/back, and retry. It now also shows queued success/error toasts for completed, already-downloaded, and failed download paths. | Firebase/R2 QA should verify toast timing with real signed package downloads and network failure retries. |
| Help center discovery | HelpCenter now includes `help.search`, category chips under `help.category.*`, filtered FAQ results, an empty search state, and keeps existing FAQ accordion/contact/refund/policy routes. | Manual QA should verify Korean query matching, category reset behavior, and mail/Safari/refund routing on device. |
| Saved filters management | SavedScreen now has `saved.search`, `saved.sort`, `saved.category.*`, edit mode, selected-state overlays, `saved.delete.selected`, and a per-tile context menu download removal action while preserving `saved.tile.<uuid>` routing. | Firebase QA should verify savedFilters listener reconciliation after bulk removal and second-device sync. Grid tiles do not use row-style swipe actions, so context menu is the per-item removal path. |
| Saved pull-to-refresh | SavedScreen now applies pull-to-refresh across loading, empty, and populated states. The empty-downloads state is inside a ScrollView so overscroll can trigger refresh, and duplicate refresh requests are ignored while one is in flight. | Firebase QA should save/download from a second device, pull on Saved, and verify filter metadata and saved/download state converge without duplicate spinners. |
| Search Dynamic Type and motion | Search cancel now uses a shorter placeholder, fixed 60pt/44pt cancel target, layout priority for the field, and a fade-only transition when Reduce Motion is enabled. Suggested keyword chips keep 44pt tap height, FlowLayout no longer counts trailing spacing as row width, and maker cards use larger combined accessibility targets. | Manual QA should verify Search at Dynamic Type `.accessibility5` and Reduce Motion enabled: cancel returns to browsing, chips wrap evenly, and maker cards remain readable in light/dark mode. |
| Search live results | Search now separates raw input from a 250ms debounced query, cancels stale debounce tasks, shows a non-blocking updating state while typing, keeps submit-based recent-search persistence, and supports pull-to-refresh on submitted results through `store.load(force: true)`. | Manual QA should type rapidly, verify only the final query updates after the delay, submit to results, pull to refresh, and confirm the keyboard dismissal behavior remains interactive. |
| Profile and detail pull-to-refresh | ProfileScreen now supports pull-to-refresh for both own and other-user profiles. Own profile refresh performs one-shot Firestore reads for profile, my filters, saved filters, and captures, then refreshes store filter metadata; other profiles reload the user document. FilterDetailScreen refreshes Cloud Function detail responses when loaded through the loader and falls back to `store.load(force: true)` for local filter detail. | Firebase QA should mutate profile counts/filter rows/reviews from another client, pull on Profile and FilterDetail, and verify visible metadata updates without duplicate refresh work. |
| Follow list pull-to-refresh | FollowersListScreen and FollowingListScreen now expose pull-to-refresh on their ScrollView. Manual refresh reloads the profile count document and follows query, then rebuilds row profiles while keeping realtime listeners attached. | Firebase QA should add/remove a follow edge from another client, pull on both list modes, and verify counts and rows converge. |
| Notifications pull-to-refresh | NotificationsInboxScreen now supports pull-to-refresh for both empty and populated states. NotificationsInboxStore can explicitly reload the first page, reset paged items, recompute load-more state, and preserve listener-based live updates. This completes the #71 refresh coverage set across Saved, Search results, Profile, Follow lists, Notifications, and FilterDetail. | Firebase QA should seed notifications, pull to refresh, mark rows read, load older pages, then pull again and verify first-page state and badge counts converge. |
| Notifications read state | Notification rows now show unread state with a left accent bar, highlighted background, and accent timestamp. The toolbar exposes `notif.markAllRead`, which batch-writes `readAt` for unread documents with optimistic local update and rollback on failure. | Firebase QA should seed read/unread rows, verify visual contrast, tap one row, use mark-all-read, and confirm Firestore `readAt` fields plus app badge count update. |
| Toast/Snackbar | FMToast now supports success/warning/error/info variants, queued auto-dismiss, Reduce Motion fade-only transition, VoiceOver announcement, swipe-up dismiss, and variant haptics. `fmToastOverlay(toast:)` remains the common attachment point used by screens such as download progress and workflow feedback. | Manual QA should trigger sequential toasts in light/dark mode, verify VoiceOver announces title/detail, swipe-up dismisses immediately, and Reduce Motion removes bottom slide motion. |
| Reduced Motion coverage | DesignSystem controls and remaining app surfaces now route animation through `reducedIfNeeded`, `fmAnimation`, explicit reduceMotion ternaries, or static skeleton fallback. Search, Saved, Collections, Help Center, Camera focus/save banner, FilterDetail tag press, and shared controls no longer use full-strength direct animations without Reduce Motion handling. | Manual QA should enable Reduce Motion and exercise search cancel, FAQ expand, collection edit, camera focus/save banner, filter swipes, tab/button presses, segmented controls, sliders, toggles, and onboarding page changes to verify motion is short or fade/static. |
| Dark mode token contrast | AppTests now resolve `FMColors` against light/dark `UITraitCollection` and verify text, accent, and semantic token contrast. The light warning token was darkened within the existing gold/brown family so semantic warning text reaches 4.5:1 on light backgrounds. | Manual QA should still inspect full screens in light/dark mode because token contrast does not catch image overlays, gradients, translucent states, or layout-specific combinations. |
| System image accessibility audit | `SYSTEM_IMAGE_A11Y_AUDIT.md` classifies all 183 `Image(systemName:)` calls across App and DesignSystem by owner context: decorative, parent-labeled, standalone button, status/metadata, or DesignSystem primitive. `SystemImageAccessibilityAuditTests` now fails when the Swift source count changes without updating the audit. | Manual VoiceOver QA should sample toolbar-only buttons, rating/status icons, decorative empty states, and DesignSystem controls to confirm labels are owned by the parent control or hidden where appropriate. |
| Before/after comparison | FilterDetail before/after now uses a 44pt scrub handle, a drag progress popover, a long-press original comparison state, and a richer VoiceOver value that announces both before/after percentages. CapturePreview does not currently expose a before/after slider because it receives only the filtered image payload. | Manual QA should drag the FilterDetail comparison on small devices and large Dynamic Type, verify the popover stays visible above the finger, long-press reveals the original state, and VoiceOver increment/decrement changes by 5%. |
| Number formatting | DesignSystem now exposes `Int.fmDecimal()`, `Int.fmCompactCount()`, and `.fmCounter()` for locale-aware counters with tabular digits and numeric transitions. FMFilterTile, FilterDetail stats/like count, Profile stats/grid counts, and Social download labels use the shared formatter instead of duplicated `K` logic. | Manual QA should switch Korean/English locales, inspect compact counters such as 980/1,200/12,400, and verify layout remains stable as counts change. |
| Marketplace card metadata | FMFilterTile now allows filter titles up to two lines with tail truncation and scale fallback, keeps maker/download metadata on one line, applies semibold title hierarchy, and reserves a stable bottom info block height for grid alignment. | Manual QA should inspect long Korean/English titles at large Dynamic Type and verify grid rows remain visually aligned with readable maker/download metadata. |
| Marketplace progressive image loading | FMRemoteImage now centralizes direct `AsyncImage` usage across DesignSystem/App remote image surfaces. FMFilterTile, avatars, detail samples, social covers, review mini covers, and moderation previews share skeleton loading, failure fallback, and rounded clipping behavior. Marketplace still prefetches the first visible trending/new cover URLs and the next eight covers as cards appear. | Network-throttled QA should open Marketplace/detail/social/moderation surfaces and scroll quickly, verifying skeletons appear immediately, fallback art appears on failures/no URL, and below-fold covers are warmed without visible card collapse. |
| Marketplace category strip | Marketplace category chips now live inside a ScrollViewReader. The selected chip scrolls into center on appear and selection change, with animation disabled when Reduce Motion is enabled and accessibility value reflecting selected/unselected state. | Manual QA should scroll the category strip, choose an off-screen category, revisit Marketplace, and verify the active chip remains visible. |
| Login button hierarchy | LoginScreen now uses a clear Apple > Google > Email hierarchy with shared 50pt height, 8pt radius, one-line Dynamic Type fallback, Apple black/white contrast by color scheme, Google account copy, and an outline email utility action. | Manual QA should inspect light/dark login screens, VoiceOver labels, Password Manager entry through email, and ensure Apple/Google flows still route through their existing handlers. |
| Profile stat affordance | Profile stat links now reserve a 52pt hit area, show a subtle chevron next to each label, use Reduce Motion-aware press scale feedback, and expose explicit VoiceOver labels/hints for filter, follower, and following list navigation. | Manual QA should tap each stat on own/other profiles and verify the correct list opens with clear VoiceOver button semantics. |
| Onboarding skip/progress | Onboarding keeps the skip CTA in the top-right on pages 1~3, adds a visible `n / 4` page count under the dots, makes interim Next secondary while the final Start CTA is primary, and uses shorter Reduce Motion-friendly animation. | Manual QA should run first-launch onboarding at large Dynamic Type and with Reduce Motion enabled, then verify skip/next/start all reach the expected screen without layout clipping. |
| Settings hierarchy | Settings now groups rows under Account, Camera Defaults, Notifications, Permissions, Data, Info, Admin, and a separated Danger section. Logout uses neutral danger styling with confirm, while Account Deletion uses red-tinted destructive styling and routes to the destructive confirm screen. | Manual QA should inspect light/dark and large Dynamic Type, verify the iOS settings permission row opens the app settings page, and confirm the danger section remains visually separated from ordinary rows. |
| Root tab badges | FMTabBar now accepts number/dot badges without shifting tab layout. RootShell listens to the latest 100 notification docs, shows unread count on Profile, announces the count through VoiceOver, and falls back to an upload-pending dot when no unread notification count is present. | Firebase QA should seed unread/read notification docs, mark all read, submit a filter to pending, and verify the Profile tab badge transitions number → none/dot with one-time haptic on new unread arrival. |
| Form input hints | FMTextField now carries shared autocapitalization and submit-label defaults, plus handle and URL convenience constructors. Profile edit, upload tags, review/report/refund text editors, refund order ID, collection naming, and generic workflow input now expose more appropriate keyboard, content type, autocapitalization, and return-key hints. | Manual QA should inspect keyboard layouts on device/simulator: URL fields show URL keyboard affordances, handle/order inputs do not auto-capitalize, search fields show Search, and multiline text still permits normal line breaks. |
| Camera Dynamic Type | CameraScreen now caps Dynamic Type at xLarge for overlay layout safety and migrates visible overlay text to dynamic camera font tokens: mode, aspect, timer, flash, swipe hints, current filter label, intensity value, carousel titles, zoom labels, countdown, and capture save banner. Fixed-size SF Symbol icon fonts remain for icon geometry. | Manual small-device QA should test default, xLarge, and accessibility text sizes, verifying camera controls do not overlap and VoiceOver labels remain unchanged. |
| Upload draft autosave | UploadCover, UploadTags, and UploadSubmit now save the current upload draft on step transition and on disappear before pending submission. Tags and Submit also expose the same cancel confirmation as Cover: save draft and leave, discard draft and leave, or keep writing. The persistence path reuses the existing editorDraft local/remote autosave plus makerDraft save. | QA should edit cover/signature/tags/category/description, navigate between steps, cancel with each option, force-quit/relaunch under the same uid, and verify the draft restores or clears according to the selected action. |
| Upload step progress | The shared upload step indicator now appears across all 4 upload states, including UploadPendingReview. The indicator exposes a single VoiceOver label/value such as "업로드 4단계 중 4단계, 검수" while preserving the visible numbered step circles. | Manual QA should navigate forward/back through Cover, Tags, Submit, and Pending and verify the visual active step and accessibility announcement match the route. |
| Empty state recovery | FMEmptyState now supports per-call CTA title overrides while preserving the default kind copy. Marketplace empty rails retry `store.load(force: true)`, Saved empty downloads routes to SearchScreen through "마켓 둘러보기", and FilterDetail not-found retries the loader through "다시 시도". | QA should force empty marketplace/saved/detail states and verify each CTA performs a visible recovery action with stable accessibility labels and no layout shift. |
| Permission denied recovery | Camera, Photos, Location, and Notifications denied screens now listen for app reactivation and re-check the relevant permission. When the user enables access in Settings and returns, the screen calls `onPermissionGranted`; CameraScreen uses that callback to re-enter authorized camera state. UI-test direct permission routes now open the iOS app settings URL instead of no-op closures. | Interactive simulator/device QA should deny each permission, tap Settings, grant permission, return to the app, and verify the denied surface dismisses or advances without a second manual retry. |
| Dynamic Type regression | UI-test host now accepts `-ui-dynamic-type` and `DynamicTypeRegressionTests` covers `accessibility5` across Search, FilterDetail, Profile, Settings, Wallet, Notifications, Upload Cover, and Camera primary controls. Search/Profile also avoid `Auth.auth()` when Firebase is intentionally disabled in UI tests. | Manual small-device QA should still inspect full visual composition at `.xxxLarge` and `.accessibility5`, especially lower FilterDetail content such as review links that can legitimately require scrolling. |
| Camera library thumbnail | CameraScreen now fetches the most recent PhotoKit image when read access is authorized/limited, renders it as the bottom-left 44pt gallery thumbnail, falls back to the existing icon for denied/empty libraries, refreshes on foreground, and preserves PhotoImport routing. | Device/simulator-with-photos QA should grant Photos access, confirm the latest photo appears, add/save a newer photo and foreground the app, then verify the thumbnail and VoiceOver date update. |
| Wallet security rules | Wallet, ledger, entitlements, Pro status, and refund request subcollections are owner-read/server-write only. | Emulator rules tests pass; production Firebase rules deploy remains a release gate. |
| Pro subscription verifier | Pro receipts are now idempotent, owner-scoped, and expired/revoked JWS metadata maps to inactive Pro state. | StoreKit sandbox renewal/cancel manual QA remains required. |
| Notifications inbox | Notification rows now compute relative time/buckets from `createdAt`, expose literal Korean labels instead of unresolved keys, support older-page loading, and surface markRead/follow errors. | Firebase QA should seed 100+ notifications and verify load-more, badge count, read state, and follow edge writes. |
| Notification settings | Category and quiet-hours changes now save through user-input bindings with an 800ms debounce, while remote listener snapshots only update local state. The system card reads actual iOS notification authorization status. | Manual QA should toggle several categories quickly and verify one final Firestore document state, then change iOS notification permission in Settings and confirm the card refreshes on return. |
| Settings/notifications polish | Settings push status now reads `MooditStore.notificationPreferences`, notification read state updates optimistically with rollback on Firestore failure, FMToast queues rapid messages, and Profile uses design tokens for card spacing/shadow and grid text color. | QA should toggle notification settings and return to Settings, tap notification rows under slow network, and trigger multiple toasts rapidly to verify sequential display. |
| Foreground push gating + in-app banner | Foreground push presentation now maps `kind`/`type`/`category` payloads to notification preferences and suppresses banner/sound during disabled categories or quiet hours. Allowed foreground pushes also show `notif.foreground.banner`, announce via VoiceOver, auto-dismiss, and tap into the payload deep-link or notifications fallback. | Device/APNs QA should send foreground pushes for like/review/wallet/system kinds with preferences on/off and quiet hours spanning the current time, then tap the in-app banner and verify deep-link routing. |
| Push device registration | FCM tokens received before login are cached, and auth state changes retry device registration or explicitly fetch the current token. | New-user device QA should verify `/users/{uid}/devices/{deviceId}` appears after login even when the token was issued before auth. |
| Push authorization actor hop | PushRegistration now hops from the notification authorization callback to `Task { @MainActor }` before calling `registerForRemoteNotifications`, matching the existing delegate callback actor isolation. | Main Thread Checker QA should request/deny/allow push permission and verify no Swift concurrency or main-thread warnings appear. |
| Login auth state | LoginScreen now updates `isAuthenticated` through its `@AppStorage` binding in a shared completion path for simulated/Google sign-in instead of writing `UserDefaults` directly. | QA should complete Google sign-in and verify RootShell, handle onboarding, and pending deep-link gates observe the authenticated state immediately. |
| Handle onboarding sheet race | RootShell now gates the empty-handle onboarding sheet once per authenticated user session. Dismissing the sheet or receiving repeated profile snapshots no longer reopens it until the next cold start/auth reset, while saving a handle still closes the sheet. | Firebase QA should sign in with an empty-handle profile, dismiss the profile sheet, trigger a foreground/background cycle or profile listener refresh, and verify the sheet does not reopen in the same session. |
| Search recent/perf | SearchScreen now persists recent searches in UID-scoped UserDefaults keys and caches popular maker grouping so text input does not recalculate the full filter list. | QA should submit a search, force-quit/relaunch, verify the recent term returns for the same user, then type in the search box and confirm input remains responsive with seeded filters. |
| Saved/favorites state | Saved filters and favorites now sync through `/users/{uid}/savedFilters` and `/users/{uid}/favorites` snapshot listeners with optimistic rollback on failed remove/favorite writes. | Firebase QA should verify download, favorite toggle, remove download, app relaunch, and second-device sync under the same uid. |
| Saved/builtin detail routing | SavedScreen and BuiltinFilters now route filter cards/info buttons with `filter.id.uuidString` instead of display titles, allowing FilterDetailLoader to hit the local UUID fast path. | QA should tap the first saved tile and a built-in filter info button and verify the detail header loads without a not-found state. |
| Collections state | FavoritesCollectionScreen now listens to `/users/{uid}/collections`, derives a non-negative custom collection count, creates/deletes collection documents directly under owner-scoped Firestore Rules, and surfaces failures through alerts. | Firebase QA should create a collection, leave and re-enter the screen, verify it persists on a second device, then delete it and confirm the document is removed. |
| Wallet optimistic reconcile | Coin credit optimism now falls back to a direct wallet balance reload if the listener does not reconcile within 10 seconds. | StoreKit/Firebase QA should verify top-up UI immediately changes, then converges to `/users/{uid}/wallet/balance.value`. |
| Wallet top-up package rows | WalletTopup package rows now use a dedicated pressed row style, 76pt stable row height, stronger semibold bonus capsules, haptic feedback on selection, and fixed 112pt trailing price/progress area so purchase progress does not resize the row. | StoreKit sandbox/manual QA should press each package, start a purchase, and verify light/dark contrast, Dynamic Type truncation, and no layout shift while the spinner is visible. |
| Wallet transaction ledger | WalletTransactions now has a wallet-specific empty state with a top-up CTA for first-time users and a clear-filter CTA when the selected transaction type has no rows. Category chips use accent-filled selected state, outlined inactive state, haptic feedback, selected accessibility value, and ledger amounts use monospaced digits. | Firebase QA should test an empty ledger, a ledger with only one transaction type, each category filter, refresh, and large Dynamic Type to verify rows and empty states remain stable. |
| Moderation detail | Moderation detail now loads the target filter document, displays cover/signature preview, metadata, tags, and engine fields, disables repeated approve/reject calls, and offers a 5-second undo through `undoModerationDecision`. | Admin Firebase QA should approve/reject a pending filter, verify queue listener removal, test undo within 5 seconds, and confirm the detail screen auto-dismisses when not undone. |
| Filter rejection recovery | FilterRejected now groups the next steps into clear bottom actions: re-edit/resubmit, support contact, and destructive filter delete confirmation. It also adds policy copy for 48-hour re-review, possible 24-hour repeated resubmission cooldown, and selectable rejection reason/moderator note text. | Manual QA should open a rejected filter, copy reason text, verify support mail/deep link, tap re-edit, and confirm destructive delete requires confirmation before dismissing. |
| Pro paid-filter access | Active Pro users now see paid filters as included in PaywallSingle and download them without `purchaseFilter` coin deduction. There is no ad SDK path in the current app build. | StoreKit sandbox QA should activate Pro, open a paid filter, confirm no coin balance change, and verify saved filter sync. |
| Paid filter signed URL entitlement | `getFilterDetail` now denies paid filter `signedDownloadURL` generation unless the caller owns `/users/{uid}/entitlements/{filterId}` or has active Pro status. | Firebase QA should call `getFilterDetail` for a paid filter as a non-owner, owner, and active Pro user, verifying `permission-denied` vs signed URL behavior. |
| Insufficient balance retry | InsufficientBalanceScreen now keeps the original `filterID`, watches live wallet balance, auto-retries `purchaseFilter` once balance is sufficient, exposes a manual `insufficient.purchase.retry` CTA, and routes to FilterAfterDownload after success. | StoreKit/Firebase QA should start a paid filter purchase with low balance, top up, return to the insufficient screen, and verify automatic or manual completion without returning to detail. |
| Payout | Current screens are marked `MockOnly`; automated QA verifies placeholder surfaces, not real Stripe payout behavior. | Keep hidden/controlled until payout backend scope is defined. |
| Firebase-backed mutations | Several screens expose state and action contracts, but production write/read behavior still depends on real Firebase setup. | Run manual QA with seeded Firebase project before release. |
