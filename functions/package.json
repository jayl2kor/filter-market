{
  "name": "moodit-functions",
  "version": "0.0.0",
  "private": true,
  "description": "moodit Cloud Functions (Firebase + TypeScript)",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "lint": "eslint --ext .ts src",
    "lint:fix": "eslint --ext .ts src --fix",
    "serve": "npm run build && firebase emulators:start --only auth,functions,firestore",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "deploy:staging": "firebase use staging && firebase deploy --only functions",
    "deploy:prod": "firebase use prod && firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "npm run build && node --test test/recordUse.test.mjs test/getFilterDetail.test.mjs test/wallet.test.mjs test/moderation.test.mjs test/identity.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project moodit-rules-test 'node --test test/firestore-rules.test.mjs'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.685.0",
    "@aws-sdk/s3-request-presigner": "^3.685.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0",
    "ioredis": "^5.4.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/node": "^20.16.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "firebase-tools": "^15.17.0",
    "typescript": "^5.5.4"
  }
}
